import { describe, it } from "node:test";

describe("TYPE-009: Weakly Typed Telemetry Options", () => {
	it.todo("_sdk is typed as SdkHandle | null instead of any");
	it.todo("_testProvider is typed as { register?(): void } instead of unknown");
});
