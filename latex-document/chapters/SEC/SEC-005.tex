\section{SEC-005: Arbitrary File Read via Path Traversal}
\label{sec:sec-005}

\subsection*{Metadata}
\begin{tabular}{ll}
\textbf{Issue ID} & SEC-005 \\
\textbf{Severity} & Medium (CVSS 6.5) \\
\textbf{Category} & Security \\
\textbf{CWE} & CWE-22: Path Traversal \\
\textbf{Affected File} & \srcfile{src/tools/shared.ts}, \srcfile{src/tools/read.ts}, \srcfile{src/tools/write.ts}, \srcfile{src/tools/edit.ts} \\
\textbf{Branch} & \branchref{fix/SEC-005-path-traversal} \\
\textbf{Changes} & \branchdiff{fix/SEC-005-path-traversal} \\
\textbf{Commit} & af00ef89ef52a46a9065a70d25307d550c4aa8ef \\
\end{tabular}

\subsection*{Executive Summary}
The \texttt{read}, \texttt{write}, and \texttt{edit} tools each contained a duplicate \texttt{resolvePath()} function that resolved user-provided paths by supporting absolute paths (\texttt{/} prefix), home directory expansion (\texttt{\textasciitilde}), and relative paths resolved against the working directory---but none of them validated that the resolved path stays within the agent's working directory. An attacker who can influence the LLM's tool arguments (via prompt injection) can direct the agent to read or write arbitrary files on the system: \texttt{/etc/passwd}, \texttt{\textasciitilde{}/.ssh/id\_rsa}, \texttt{/etc/shadow}, and any other file the agent process has permission to access.

The fix consolidates path resolution into a single \texttt{resolveSafePath()} function in \srcfile{src/tools/shared.ts} that performs an allowlist-based validation: it resolves the path relative to the working directory and then verifies the resolved path is within the allowed base directory using a relative-path comparison. Both absolute paths and \texttt{../} traversal sequences that escape the workspace are rejected.

\subsection*{Root Cause Analysis}
The duplicate \texttt{resolvePath()} function appeared identically in three files. In \srcfile{src/tools/read.ts}:

\begin{lstlisting}[language=TypeScript]
function resolvePath(path: string, cwd: string): string {
    if (path.startsWith("~/") || path === "~") {
        path = homedir() + path.slice(1);
    }
    return path.startsWith("/") ? path : resolve(cwd, path);
}
\end{lstlisting}

The function handles three path types: home directory expansion (replaces \texttt{\textasciitilde} with \texttt{os.homedir()}), absolute paths (passes the path through unchanged), and relative paths (resolves against \texttt{cwd}). There is no validation that the resolved path is within \texttt{cwd} or any allowed directory. The resolved path is used directly in \texttt{readFile()}, \texttt{writeFile()}, and \texttt{readFile()}/\texttt{writeFile()} pairs, allowing access to SSH private keys, cloud credentials, Kubernetes configuration, and system files.

\subsection*{Fix Implementation}
The fix introduces a single \texttt{resolveSafePath()} function in \srcfile{src/tools/shared.ts} that validates the resolved path stays within the workspace:

\begin{lstlisting}[language=TypeScript]
export function resolveSafePath(path: string, cwd: string): string {
    if (path.startsWith("~/") || path === "~") {
        path = homedir() + path.slice(1);
    }

    const resolved = path.startsWith("/") ? path : resolve(cwd, path);
    const allowedBase = resolve(cwd);
    const rel = relative(allowedBase, resolved);

    if (rel.startsWith("..") || path.startsWith("/")) {
        throw new Error(
            \texttt{Path traversal detected: "\${path}" resolves outside the working directory. } +
            \texttt{Use paths relative to the workspace: \${cwd}},
        );
    }

    return resolved;
}
\end{lstlisting}

The three duplicate \texttt{resolvePath()} functions are removed from \texttt{read.ts}, \texttt{write.ts}, and \texttt{edit.ts} and replaced with a shared import of \texttt{resolveSafePath}. The schema descriptions in \texttt{shared.ts} are updated to document the restriction:

\begin{lstlisting}[language=TypeScript]
export const readSchema = Type.Object({
    path: Type.String({
        description: "File path relative to the working directory. " +
                     "Cannot use absolute paths or ../ traversal.",
    }),
    // ...
});
\end{lstlisting}

\subsection*{Affected Files}
\begin{itemize}
    \item \srcfile{src/tools/shared.ts} --- New \texttt{resolveSafePath()} function added; schema descriptions updated to document path restrictions.
    \item \srcfile{src/tools/read.ts} --- Removed duplicate \texttt{resolvePath()}, replaced with shared \texttt{resolveSafePath()}.
    \item \srcfile{src/tools/write.ts} --- Removed duplicate \texttt{resolvePath()}, replaced with shared \texttt{resolveSafePath()}.
    \item \srcfile{src/tools/edit.ts} --- Removed duplicate \texttt{resolvePath()}, replaced with shared \texttt{resolveSafePath()}.
    \item \srcfile{src/__tests__/path-traversal.test.ts} --- New test suite for \texttt{resolveSafePath()}.
\end{itemize}

\subsection*{Verification}
A comprehensive test suite validates all path traversal scenarios: absolute paths (\texttt{/etc/passwd}) are rejected, home directory expansion (\texttt{\textasciitilde{}/.ssh/id\_rsa}) is rejected, \texttt{../} traversal sequences (\texttt{../../../etc/passwd}) are rejected, deeply nested traversal (\texttt{subdir/../../other}) is rejected, while valid relative paths (\texttt{file.txt}, \texttt{subdir/file.txt}) and deeply nested valid paths (\texttt{a/b/c/d/file.txt}) are correctly resolved within the workspace.

\subsection*{Test File}
\srcfile{src/__tests__/path-traversal.test.ts}

Contains 10 test cases covering absolute path rejection, home directory expansion rejection, \texttt{../} traversal rejection (both shallow and deeply nested), valid relative path resolution, subdirectory traversal, deeply nested valid paths, tilde-only expansion, and absolute paths with trailing slashes.
